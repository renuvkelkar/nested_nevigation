import 'package:flutter/material.dart';

class Subpagebase extends StatelessWidget {
  const Subpagebase({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 600,
      color: Color.fromARGB(255, 119, 229, 241),
    );
  }
}
